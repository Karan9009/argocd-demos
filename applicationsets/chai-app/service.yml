apiVersion: v1
kind: Service
metadata:
  name: chai-app-service
spec:
  selector:
    app: chai-app
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    nodePort: 30080
  type: NodePort
